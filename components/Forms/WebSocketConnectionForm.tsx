import { View } from "native-base";
import CustomModal from "../ui/Modal";

export default function WSTestConnectionForm({ id,
  showForm,
  setShowForm
}: {
  id: string;
}) {
  return (
    <CustomModal>

    </CustomModal>
  )
}
