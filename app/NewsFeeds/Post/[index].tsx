import { View } from "native-base";
import { Text } from "native-base";


export default function Post({ postId }: {
  postId: string
}) {
  return (
    <View>
      <Text>Post</Text>
    </View>
  );
}
